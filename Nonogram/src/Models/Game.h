#pragma once
#include "Screens/Screen.h"

////////////////////////////////////////////////////////////
//	Class descrining game state
////////////////////////////////////////////////////////////
class Game {
public:
	Game(Screen* firstScreen, std::string title, int width = 1080, int height = 720);

	////////////////////////////////////////////////////////////
	//	It's used to change displayed screen
	////////////////////////////////////////////////////////////
	void changeScreen(Screen*);

	////////////////////////////////////////////////////////////
	//	Method used to show current screen.
	////////////////////////////////////////////////////////////
	void show();

private:
	int width, height;
	Screen* screen;
	Context* context;
};